defmodule Regitry do

  alias EchoServer

  def hello do

    :world
  end
end



 # Registry.start_link(name: :my_registry, keys: :unique)
 # EchoServer.start_link("server one")
 # EchoServer.start_link("server two")
